import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class CrawlerService {

  constructor() {
    // TODO: is this even possible done via browser? CORS prevents this. This might need a server
  }
}
